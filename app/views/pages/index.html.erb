<p style="color: green"><%= notice %></p>

<h1>Pages</h1>

<div id="pages">
  <% @pages.each do |page| %>
    <p>
      <%= link_to page.slug, page %>
    </p>
  <% end %>
</div>

<%= link_to "New page", new_page_path %>
